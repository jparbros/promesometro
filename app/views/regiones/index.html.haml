- content_for :title, ":: Directorio"

%h1
  Selecciona una región

#directory
  .directory_title
    %h1 Directorio
  .region_title
    %h1 Provincias
  .official_title
    %h1 Autoridad
  .mapa
    = render :partial => 'shared/mapa'
  .region
    &nbsp;
  .representante
    &nbsp;

%script{:type => "text/template", :id => "provincia-template"}
  <a href="#representante/<%= name %>/alcalde"><%= name %></a>

%script{:type => "text/template", :id => "estado-template"}
  <a href="#representante/<%= name %>/gobernador"><%= name %> &gt;&gt;</a>

%script{:type => "text/template", :id => "representante-template"}
  .representante_bar
    <img src="<%= avatar_url %>" class="foto_representante">
    %span.representante_nombre
      <%= name %>
  .representante_bar
    <%= political_party_name %>
    <br/>
    <%= place %>
  .representante_bar
    <a href="<%= resume %>" target="_blank">Hoja de vida</a>
  .representante_bar
    <a href="/promesas/official/<%=name%>"><%=promise_count%> Promesas</a>