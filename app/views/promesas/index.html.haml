#promesas
  %h1
    Promesas

  - titulo_filtro_promesas(@search.search_attributes)

  = render :partial => 'promesa', :collection => @promises
  - if @promises.empty?
    %h3
      No existen coincidencias para su busqueda